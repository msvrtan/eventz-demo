{% extends "::base.html.twig" %}

{% block jumbotron %}
{% endblock %}

{% block body %}
    <div class="container">

        {% for event in events %}
            <div class="row">
                <div class="col-md-12">
                    <p>{{ event.name }}</p>
                    <a href="{{ path('company_eventz_page',{id:event.id}) }}">More about {{ event.name }}</a>
                </div>
            </div>
        {% endfor %}

    </div>

{% endblock %}
